# ICE40 FPGA backend

Place ICE40-specific BSV primitive wrappers and RTL implementations in this directory. Board constraints, clocking, top-level integration, and programming settings belong under `boards/<board>/`.
